<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".sponsorquiz.QuizEnd">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/endMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="@string/quiz_completion_message"
        android:textSize="20pt"/>

    <Button
        android:id="@+id/endButton"
        android:layout_width="@dimen/fui_auth_method_button_width"
        android:layout_height="@dimen/fui_auth_method_button_width"
        app:layout_constraintTop_toBottomOf="@+id/endMessage"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:text="Go back to the Events"/>

</androidx.constraintlayout.widget.ConstraintLayout>